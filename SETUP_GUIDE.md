# دليل تشغيل الموقع على أي جهاز جديد (VS Code)

هذا الملف مخصص لك كمرجع شخصي لكي تتمكن من فتح وتشغيل هذا الموقع من أي كمبيوتر آخر بسهولة تامة.

## 1️⃣ البرامج التي ستحتاج تحميلها أولاً:
قبل أن تبدأ، يجب أن تتأكد من وجود هذه البرامج على الجهاز الجديد:
1. **برنامج المحرر (VS Code):** لتحميله [اضغط هنا](https://code.visualstudio.com/Download)
2. **برنامج (Node.js):** وهو ضروري جداً لتشغيل المكتبات وسيرفر التطوير. لتحميله [اضغط هنا](https://nodejs.org/) (قم بتحميل النسخة التي مكتوب بجوارها LTS).

---

## 2️⃣ خطوات فتح المشروع:
1. قم بتحميل ملفات المشروع (أو سحبها من GitHub) وضعها في مجلد على الكمبيوتر.
2. افتح برنامج **VS Code**.
3. من القائمة العلوية اضغط على `File` ثم اختر `Open Folder` واختر مجلد المشروع (مجلد Cars).

---

## 3️⃣ خطوات تثبيت المكتبات وتشغيل الموقع:
بمجرد فتح المشروع داخل VS Code، ستحتاج لفتح "موجة الأوامر" (Terminal) داخل البرنامج:
1. من القائمة العلوية في VS Code، اضغط على `Terminal` ثم اختر `New Terminal`.
2. سيفتح لك مربع بالأسفل لكتابة الأوامر.

**الأمر الأول (لتحميل المكتبات):**
قم بنسخ هذا الأمر ولصقه في التيرمينال ثم اضغط Enter:
```bash
npm install
```
*(هذا الأمر سيقوم بإنشاء مجلد `node_modules` وتحميل كل الأدوات اللازمة مثل Vite وغيرها. انتظر حتى ينتهي).*

**الأمر الثاني (لتشغيل السيرفر والموقع):**
بعد انتهاء التحميل، انسخ هذا الأمر واضغط Enter:
```bash
npm run dev
```
*(هذا الأمر سيقوم بتشغيل سيرفر التطوير الخاص بـ Vite).*

---

## 4️⃣ عرض الموقع في المتصفح:
بعد كتابة `npm run dev`، سيظهر لك في التيرمينال رابط بهذا الشكل:
`http://localhost:5173/`

- يمكنك ببساطة الضغط على زر `Ctrl` في لوحة المفاتيح والضغط بالماوس على الرابط ليفتح مباشرة في المتصفح.
- أي تعديل ستقوم به على الكود (HTML, CSS, JS) وتقوم بحفظه (`Ctrl + S`)، سيظهر فوراً في المتصفح بدون الحاجة لعمل Refresh!

بالتوفيق! 🚀
